---
{}
---
#### Your Final Report

|   |   |
|---|---|
|Total marks|28|
|Total Questions|25|
|Questions correctly answered|17|
|Success ratio|68%|
|Marks secured|20|
|Percentage secured|71.43%|
1)  A network command-line utility in MS Windows that tracks and displays the route taken by IPv4 packets on their way to another host is called:

-    ping 
-    traceroute ( Your answer) X UNIX
-    nslookup
-    ==tracert ( Missed)==

2)  What is the name of a Windows command-line utility that can be used to display TCP/IP configuration settings?

-    ifconfig ( Your answer) X LINUX
-    nslookup
-    ==ipconfig ( Missed)== windows
-    netstat

3)  The Linux command-line utility for network interface configuration is called:

-    ==ifconfig ( Missed)== X linux
-    netstat
-    nslookup
-    ipconfig ( Your answer)== windows

4)  Which network command-line utility in MS Windows combines the features of ping and tracert?

-    nbtstat
-    ==pathping ( Missed)==
-    traceroute
-    netstat ( Your answer) X

5)  Which of the following answers refers to a command-line tool used for security auditing and testing of firewalls and networks?

-    pathping
-    netstat ( Your answer) X generates display that show network status and protocol statistics
-    nslookup
-    ==hping ( Missed)== hping is an open-source packet generator and analyzer for the TCP/IP protocol

6)  Which of the following answers refers to a network debugging and exploration tool that can read and write data across TCP or UDP connections?

-    ==netcat ( Missed)== netcat is a computer networking utility for reading from and writing to network connections using TCP or UDP.
-    theHarvester
-    Cuckoo
-    tcpdump ( Your answer) X

7)  Which of the following command-line commands in MS Windows are used to display the contents of a routing table? (Select 2 answers)

-    netstat -p
-    ==route print ( Missed)
-    netstat -a
-    print route ( Your answer) X think this is linux
-    ==netstat -r ( Your answer)

8)  Which of the following tools hides attacker's identity by utilizing a proxy for port scanning?

-    tcpdump
-    Cuckoo ( Your answer) X **a tool that is used to launch malware in a secure and isolated environment**,
-    ==scanless ( Missed)== 
-    dnsenum

-------------

*  Which of the following terms is used to describe a text message containing system information details displayed after connecting to a service on a server? Banner ( Your answer)

*  The practice of connecting to an open port on a remote host to gather more information about its configuration is known as: Banner grabbing ( Your answer)

*  A Linux command-line command for displaying routing table contents is called: netstat -r ( Your answer)
 

 